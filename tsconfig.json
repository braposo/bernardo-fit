{
  "//": "Trigger's CLI bundles the TypeScript tasks; Vercel builds api/*.js untouched. The admin JSX has a separate esbuild/Tailwind build. The @ alias locates its shadcn components; the included TypeScript files remain Trigger-only.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "baseUrl": ".",
    "paths": { "@/*": ["./src/admin/*"] },
    "lib": ["ES2023"],
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "noEmit": true
  },
  "include": ["trigger.config.ts", "src/**/*.ts"]
}
